#include<stdio.h>
#include<stdio.h>

int main()
{
  int num1 , num2 , sum ;
  
  printf("Enter two number:");
  scanf("%d%d",&num1 ,&num2);
  
  sum = num1 + num2;
  
  printf("Addition = %d", sum);
  
  getch();
  return 0;
}
tf("\n Addition => %d + %d =%d",No1, No2,sum);
  
  getch();
  return 0;
}