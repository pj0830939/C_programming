#include<stdio.h>
#include<stdio.h>

int main()
{
  float No = 0.0 , sqr = 0.0;
  
  
  printf("\n Enter a number=");
  scanf("%f",&No );
  
  sqr = No * No;
  
  printf("\n square of given number is %2f", sqr);
  
  getch();
  return 0;
}
on => %d + %d =%d",No1, No2,sum);
  
  getch();
  return 0;
}